<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Account Form View -->
    <record id="view_moneta_account_form" model="ir.ui.view">
        <field name="name">moneta.account.form</field>
        <field name="model">moneta.account</field>
        <field name="arch" type="xml">
            <form string="Account">
                <header>
                    <button name="action_reconcile_statement" type="object" string="Reconcile Statement" class="btn-primary" icon="fa-check-square-o"/>
                    <button name="action_import_file" type="object" string="Import CSV / QIF" class="btn-secondary" icon="fa-upload"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_transactions" type="object" class="oe_stat_button" icon="fa-list">
                            <field name="current_balance" widget="statinfo" string="Balance"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="account_type"/>
                            <field name="currency_id"/>
                            <field name="institution_id"/>
                            <field name="institution_name"/>
                            <field name="account_number"/>
                        </group>
                        <group>
                            <field name="opening_balance" widget="monetary"/>
                            <field name="opening_balance_date"/>
                            <field name="current_balance" widget="monetary" readonly="1"/>
                            <field name="cleared_balance" widget="monetary" readonly="1"/>
                            <field name="credit_limit" widget="monetary" invisible="account_type != 'credit_card'"/>
                            <field name="interest_rate" invisible="account_type not in ('loan', 'mortgage', 'loc', 'savings')"/>
                            <field name="fx_fee_percent" invisible="account_type not in ('checking', 'chequing','savings','credit_card','loc','cash')"/>
                            <field name="is_favourite"/>
                            <field name="is_closed"/>
                            <field name="closed_date" invisible="not is_closed"/>
                            <field name="exclude_from_net_worth"/>
                        </group>
                    </group>
                    <!-- 90-Day Cashflow Balance Forecast -->
                    <div class="card border-0 shadow-sm rounded-3 bg-light p-3 my-3">
                        <h6 class="fw-bold text-dark mb-2"><i class="fa fa-line-chart text-primary me-2"/>90-Day Balance Forecast (Based on Scheduled Bills &amp; Income)</h6>
                        <div class="row g-2">
                            <div class="col-4 text-center border-end">
                                <span class="text-muted small">In 30 Days</span>
                                <h4 class="fw-bold mb-0 text-dark"><field name="forecast_balance_30d" widget="monetary"/></h4>
                            </div>
                            <div class="col-4 text-center border-end">
                                <span class="text-muted small">In 60 Days</span>
                                <h4 class="fw-bold mb-0 text-dark"><field name="forecast_balance_60d" widget="monetary"/></h4>
                            </div>
                            <div class="col-4 text-center">
                                <span class="text-muted small">In 90 Days</span>
                                <h4 class="fw-bold mb-0 text-dark"><field name="forecast_balance_90d" widget="monetary"/></h4>
                            </div>
                        </div>
                    </div>

                    <group string="Credit Card Statement Cycle &amp; Due Dates" invisible="account_type != 'credit_card'">
                        <group>
                            <field name="billing_cycle_day"/>
                            <field name="payment_due_day"/>
                        </group>
                        <group>
                            <field name="next_statement_date" readonly="1"/>
                            <field name="next_payment_due_date" readonly="1"/>
                            <field name="current_cycle_spent" widget="monetary" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Register &amp; Transactions" name="transactions_page">
                            <field name="transaction_ids">
                                <list editable="bottom" default_order="transaction_date desc, id desc" decoration-muted="state == 'void'">
                                    <field name="transaction_date" string="Date"/>
                                    <field name="check_number" string="Check #" optional="show"/>
                                    <field name="payee_id" string="Payee"/>
                                    <field name="category_id" string="Category" options="{'no_create': True}"/>
                                    <field name="memo" string="Memo"/>
                                    <button name="action_toggle_cleared" type="object" string="Clr" 
                                            title="Click to toggle: Unreconciled -> Cleared -> Reconciled"
                                            icon="fa-check-circle"
                                            invisible="state == 'void'"
                                            class="btn-link"/>
                                    <field name="state" string="Status" widget="badge" 
                                           decoration-info="state == 'cleared'" 
                                           decoration-success="state == 'reconciled'" 
                                           decoration-muted="state == 'void'"/>
                                    <field name="amount" widget="monetary" sum="Total Amount" decoration-danger="amount &lt; 0" decoration-success="amount &gt; 0"/>
                                    <field name="running_balance" string="Balance" widget="monetary" optional="show"/>
                                    <field name="currency_id" column_invisible="1"/>
                                </list>
                            </field>
                        </page>
                        <page string="Sharing &amp; Joint Access" name="sharing_page">
                            <field name="share_ids">
                                <list editable="bottom">
                                    <field name="user_id"/>
                                    <field name="permission" widget="badge"/>
                                    <field name="is_joint"/>
                                    <field name="exclude_from_net_worth"/>
                                </list>
                            </field>
                        </page>
                        <page string="Notes" name="notes_page">
                            <field name="notes" placeholder="Account notes or remarks..."/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Account List View (Odoo 18 uses list) -->
    <record id="view_moneta_account_tree" model="ir.ui.view">
        <field name="name">moneta.account.list</field>
        <field name="model">moneta.account</field>
        <field name="arch" type="xml">
            <list string="Accounts" 
                  decoration-danger="account_type in ('loan', 'mortgage')" 
                  decoration-success="account_type in ('checking', 'chequing', 'savings', 'cash')"
                  decoration-info="account_type in ('credit_card', 'loc')">
                <field name="is_favourite" widget="boolean_toggle"/>
                <field name="name"/>
                <field name="institution_name"/>
                <field name="account_type" widget="badge" 
                       decoration-danger="account_type in ('loan', 'mortgage')" 
                       decoration-success="account_type in ('checking', 'chequing', 'savings', 'cash')"
                       decoration-info="account_type in ('credit_card', 'loc')"
                       decoration-primary="account_type == 'brokerage'"/>
                <field name="currency_id"/>
                <field name="current_balance" widget="monetary" sum="Total Balance"
                       decoration-danger="account_type in ('loan', 'mortgage') or current_balance &lt; 0"
                       decoration-success="account_type in ('checking', 'chequing', 'savings', 'cash') and current_balance &gt;= 0"
                       decoration-info="account_type in ('credit_card', 'loc')"/>
                <field name="cleared_balance" widget="monetary"/>
            </list>
        </field>
    </record>

    <!-- Account Kanban View with Color-Coded Boxes (Red for Mortgage/Loan, Green for Savings/Checking, Blue for Credit Card) -->
    <record id="view_moneta_account_kanban" model="ir.ui.view">
        <field name="name">moneta.account.kanban</field>
        <field name="model">moneta.account</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_order="account_type, name">
                <field name="name"/>
                <field name="account_type"/>
                <field name="currency_id"/>
                <field name="current_balance"/>
                <field name="institution_name"/>
                <templates>
                    <t t-name="kanban-box">
                        <t t-set="is_mortgage_loan" t-value="['mortgage', 'loan'].includes(record.account_type.raw_value)"/>
                        <t t-set="is_cash" t-value="['checking', 'chequing', 'savings', 'cash'].includes(record.account_type.raw_value)"/>
                        <t t-set="is_card" t-value="['credit_card', 'loc'].includes(record.account_type.raw_value)"/>
                        
                        <t t-set="card_style" t-value="is_mortgage_loan ? 'border: 2px solid #E74C3C; background: linear-gradient(180deg, #FDEDEC 0%, #FFFFFF 100%);' : (is_cash ? 'border: 2px solid #2ECC71; background: linear-gradient(180deg, #EAFAF1 0%, #FFFFFF 100%);' : (is_card ? 'border: 2px solid #2980B9; background: linear-gradient(180deg, #EBF5FB 0%, #FFFFFF 100%);' : 'border: 2px solid #8E44AD; background: linear-gradient(180deg, #F4ECF7 0%, #FFFFFF 100%);'))"/>
                        <t t-set="badge_class" t-value="is_mortgage_loan ? 'bg-danger text-white' : (is_cash ? 'bg-success text-white' : (is_card ? 'bg-primary text-white' : 'bg-secondary text-white'))"/>
                        <t t-set="balance_color" t-value="is_mortgage_loan ? 'text-danger' : (is_cash ? 'text-success' : (is_card ? 'text-primary' : 'text-dark'))"/>
                        <t t-set="icon_name" t-value="record.account_type.raw_value == 'mortgage' ? 'fa-home' : (record.account_type.raw_value == 'loan' ? 'fa-car' : (record.account_type.raw_value == 'credit_card' ? 'fa-credit-card' : (record.account_type.raw_value == 'savings' ? 'fa-money' : (record.account_type.raw_value in ('checking', 'chequing') ? 'fa-bank' : 'fa-line-chart'))))"/>

                        <div class="oe_kanban_global_click o_kanban_card rounded-3 shadow-sm p-3 mb-2" t-att-style="card_style">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="d-flex align-items-center">
                                    <div t-attf-class="rounded-circle p-2 me-2 d-flex align-items-center justify-content-center #{badge_class}" style="width: 34px; height: 34px;">
                                        <i t-attf-class="fa #{icon_name}"/>
                                    </div>
                                    <div>
                                        <strong class="o_primary fs-6 text-dark"><field name="name"/></strong>
                                        <div class="text-muted small"><field name="institution_name"/></div>
                                    </div>
                                </div>
                                <span t-attf-class="badge rounded-pill text-uppercase px-2 py-1 #{badge_class}">
                                    <field name="account_type"/>
                                </span>
                            </div>
                            <div class="d-flex justify-content-between align-items-end mt-3 pt-2 border-top">
                                <span class="text-muted small fw-semibold">Current Balance</span>
                                <h3 t-attf-class="mb-0 fw-bold #{balance_color}">
                                    <field name="current_balance" widget="monetary"/>
                                </h3>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Account Action -->
    <record id="action_moneta_account" model="ir.actions.act_window">
        <field name="name">Accounts</field>
        <field name="res_model">moneta.account</field>
        <field name="view_mode">kanban,list,form</field>
    </record>
</odoo>
